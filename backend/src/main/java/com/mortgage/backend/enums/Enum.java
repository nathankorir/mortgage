package com.mortgage.backend.enums;

public class Enum {
    public enum ApplicationStatus {
        PENDING,
        APPROVED,
        REJECTED,
        CANCELLED
    }

    public enum DecisionType {
        APPROVED,
        REJECTED
    }

}
